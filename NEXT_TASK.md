# Next Task

## Current Goal
End-to-end verify all WP-connected pages (stats, search, coins, countries, series).

## Verify Next
- `/series` and `/series/german-federal-states` load correctly
- API offline fallback works across all service methods
- `npm run build` passes
